box: wercker/default
build:
    steps:
        - arjen/hugo-build:
            theme: robust
            flags: --buildDrafts=true
deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GIT_TOKEN
            basedir: public
            repo: RomTin/RomTin.github.io
